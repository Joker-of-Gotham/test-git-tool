name: CodeQL
on:
  pull_request:
    branches: [master]
  schedule:
    - cron: '0 0 * * 0'
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "CodeQL analysis skipped"
